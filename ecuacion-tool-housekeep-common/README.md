# ecuacion-tool-housekeep-common

## What is it?

`ecuacion-tool-housekeep-common` is an internal common module shared by the `housekeep-*` tools (`ecuacion-tool-housekeep-db`, `ecuacion-tool-housekeep-files`). It is not intended to be used standalone.
