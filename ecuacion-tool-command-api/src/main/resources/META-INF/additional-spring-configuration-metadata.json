{"properties": [
  {
    "name": "jp.ecuacion.tool.command-api.allow-insecure-access",
    "type": "java.lang.Boolean",
    "description": "When true, GET requests to executeScript are allowed and POST requests skip apiKey verification (intended for trusted internal networks). Defaults to false (secure) when unset; leaving it unset is logged as a warning at startup."
  },
  {
    "name": "jp.ecuacion.tool.command-api.api-key-file-path",
    "type": "java.lang.String",
    "description": "Path to the file containing the shared secret compared against the 'apiKey' POST parameter of executeScript. Supports ${ENV_VAR} resolution."
  },
  {
    "name": "jp.ecuacion.tool.command-api.api-key-comparison-mode",
    "type": "java.lang.String",
    "description": "How every key in the api-key file is compared: PLAIN or BCRYPT. Applies to the whole file, not per line. Defaults to PLAIN when unset; an unrecognized value is logged as a warning and treated as PLAIN."
  }
]}
