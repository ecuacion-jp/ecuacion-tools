# This module's own version, read via VersionUtil.getVersion(""). Kept here rather than in
# ecuacion-tool-housekeep-common because that module is also depended on by
# ecuacion-references-tools, which needs an unambiguous classpath for its own
# version.properties (see the comment on ecuacion-tool-housekeep-common's pom.xml
# <resources> for the full reasoning).
version=@project.version@
